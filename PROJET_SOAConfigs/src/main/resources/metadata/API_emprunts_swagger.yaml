openapi: 3.0.1
info:
  title: API_emprunts
  description: API Definition of API_emprunts
  version: 1.0.0
servers:
- url: http://localhost:8290/emprunts
paths:
  /Ajout:
    post:
      requestBody:
        description: Sample Payload
        content:
          application/json:
            schema:
              type: object
              properties:
                payload:
                  type: object
        required: false
      responses:
        default:
          description: Default response
